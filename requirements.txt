numpy==1.26.4
matplotlib==3.8.4
uproot==5.6.3
awkward==2.8.5

python -m venv myenv (on ubantu)
source myenv/bin/activate (on ubantu)
source venv/bin/activate (on mac)
pip install -r requirements.txt

